using UnityEngine;
using UnityEngine.UI;
using UnityEngine.SceneManagement;

public class UiSetting : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void OnSceneLoaded(Scene scene, LoadSceneMode mode)
    {
        /*
        if (StageManager.playing)
        {
            GameObject.Find("UiCanvas").transform.GetChild(2).gameObject.SetActive(true);
            GameObject.Find("UiCanvas").transform.GetChild(3).gameObject.SetActive(true);
        }
        else
        {
            GameObject.Find("UiCanvas").transform.GetChild(2).gameObject.SetActive(false);
            GameObject.Find("UiCanvas").transform.GetChild(3).gameObject.SetActive(false);

        }
        */
    }
}
