using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.SceneManagement;

public class InitUi : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        /*
        gameObject.transform.GetChild(0).transform.localPosition = new Vector3(-200, -350, 0); //START
        gameObject.transform.GetChild(1).transform.localPosition = new Vector3(200, -350, 0); //EXIT
        gameObject.transform.GetChild(4).transform.localPosition = new Vector3(1000, -450, 0); //BACK
        

        gameObject.transform.GetChild(0).gameObject.SetActive(true); //START
        gameObject.transform.GetChild(1).gameObject.SetActive(true); //EXIT
        gameObject.transform.GetChild(5).gameObject.SetActive(true); //Login
        
        GameObject.Find("UiCanvas").transform.GetChild(3).gameObject.SetActive(false); //Time
        */
    }
    
// Update is called once per frame
void Update()
    {
        
    }
}
